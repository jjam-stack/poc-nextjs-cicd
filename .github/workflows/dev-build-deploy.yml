name: Deploy to Cloud Run (DEV)

on:
  push:
    branches:
      - dev

jobs:
  deploy-dev:
    uses: ./.github/actions/gcp-build-and-deploy.yml@${{ github.ref_name }}
    with:
      environment: dev