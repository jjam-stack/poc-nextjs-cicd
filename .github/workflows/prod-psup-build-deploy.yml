name: Deploy to Cloud Run (PROD & PSUP)

on:
  push:
    branches:
      - main

jobs:
  deploy-prod:
    uses: ../actions/deploy.yml
    with:
      environment: prod

  deploy-psup:
    uses: ../actions/deploy.yml
    needs: deploy-prod
    with:
      environment: psup